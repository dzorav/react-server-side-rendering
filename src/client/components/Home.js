import React from "react";

const Home = () => {
    return (
        <div>
            <button onClick={() => alert("Clicked!")}>Click Me!</button>
            <div>Home Csdfomponent</div>
        </div>
    )
}

export default Home